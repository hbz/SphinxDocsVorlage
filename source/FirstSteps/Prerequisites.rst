Prerequisites
=============

Play-Framework
--------------

Installation of Play framework is to set up a directory wherein the Scala activator is to be downloaded. activator comprises functionality to check 
for dependencies and update itself if needed.

Java JDK
--------

Version similar to or greater as 1.8 is required. Until now we haven't found limitations by usind OpenJDK instead of Oracle's JDK

